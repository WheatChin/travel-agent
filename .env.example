# Future SQLite server capability (local file under an ignored data directory)
DATABASE_PATH=

# Future AMap browser capability (public, domain-restricted web key)
NEXT_PUBLIC_AMAP_WEB_KEY=

# Future AMap serviceHost proxy (not integrated; server-only, never in browser bundle)
AMAP_SECURITY_JS_CODE=

# Future AMap server capability (Web Service key; never use the browser key here)
AMAP_WEB_SERVICE_KEY=

# Future model server capability
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=

# Future web-research server capability
WEB_RESEARCH_API_KEY=
